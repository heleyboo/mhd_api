<form method="post" action="{{ path('booking_delete', {'sid': booking.sid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking.sid) }}">
    <button class="btn">Delete</button>
</form>
